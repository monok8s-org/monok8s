# Minimal CRD for XTenantDatabase — flat CRD (not Crossplane XRD).
# See xtenants.monok8s.io.yaml for the rationale.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: xtenantdatabases.monok8s.io
spec:
  group: monok8s.io
  scope: Cluster
  names:
    plural: xtenantdatabases
    singular: xtenantdatabase
    kind: XTenantDatabase
    listKind: XTenantDatabaseList
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
      # No subresources — see xtenants.monok8s.io.yaml's note.
